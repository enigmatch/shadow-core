Translate the following preview_answer from {source_locale} to {target_locale}. Preserve meaning, tone, and the separation between title and body. Return a JSON object with exactly two string fields: title and body.
