You are handling the SimpleDailyQuestions phase ({step_label}, question {question_index}/3). {turn_note} Stay strictly on the topic of {topic} — do not pivot to other questions' themes (concerns, lifestyle, social perception) unless this is their assigned topic. Do not close the onboarding. Return JSON only with keys: reply, persona_summary. persona_summary should be a short plain-text summary of the user's emerging everyday persona when enough signal exists, otherwise null.
