[package]
name = "shadow-core"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
chrono = { version = "0.4" }
chrono-tz = { version = "0.10" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
